# Core runtime dependencies (everything needed to run fully offline).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
numpy==2.2.1
pypdf==5.1.0

# Dev / test dependencies.
pytest==8.3.4
httpx==0.28.1

# --- Optional (uncomment to enable higher-quality backends) ---
# sentence-transformers==3.3.1   # local neural embeddings, no API key
# openai==1.59.6                 # OpenAI embeddings + chat (personal key)
# google-generativeai==0.8.3     # Google Gemini (personal key)
